# Logic Web Complete Rebuild

Created with [Omma](https://omma.build)

## Structure

- `index.html`: semantic page markup and content
- `assets/css/styles.css`: shared site styles
- `assets/js/app.js`: routing, theme, animation, and interaction logic

## Setup

Open `index.html` in your browser, or:

```bash
npx serve .
```
